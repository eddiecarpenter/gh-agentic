name: TEST — Project status trigger

on:
  project_item:
    types: [edited]

jobs:
  dump-payload:
    runs-on: ubuntu-latest
    steps:
      - name: Dump event payload
        run: echo '${{ toJSON(github.event) }}'
