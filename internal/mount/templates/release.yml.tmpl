name: Release

on:
  release:
    types: [published]

jobs:
  release:
    uses: eddiecarpenter/gh-agentic/.github/workflows/release.yml@{{.Version}}
    secrets: inherit
    permissions:
      contents: write
